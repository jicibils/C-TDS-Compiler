class main{
   float c;

   void alo(bool b, integer  x){
      if ( b ) {
            x= x + 1;
      }
      else{
          x = x - 1;
      }  
      return ; 
   }

   integer  alo2(integer  x){
      alo3(true, x); //alo3 no se encuentra definida.
      x=  x + 1; 
      return x; 
   }
 

   integer  alo3(bool b,integer  x){
      return x*x*x*x; 
   }

   void main(){
        integer  y;
        y = 7;
        c = y *8;
        y = alo2(y); 
        alo(false, (c+3)*y); 
        return;
   }

}
