class main{

float promedio (float nota1, float nota2, float nota3){

	if ((nota1 > nota3) && (nota2 > nota3)){
		return (nota1 + nota2)/2.0;
	}
	if ((nota1 > nota2) && (nota3 > nota2)){
		return (nota1 + nota3)/2.0;
	}
	if ((nota2 > nota 3) && (nota3 > nota1)){
		return (nota3 + nota2)/2.0;
	}
	if (!(nota1 == nota2) || (nota1 != nota3) || !(nota2) ){ //ERROR ! tiene q tomar arg de tipo bool
		return nota1;
	}

}

bool  pruPrec(bool  b, bool  b_2){
  return (true || false && true || !b || !!b_2 );

}

void printF(float f)extern;
void printB(bool  b) extern;

/*
Saca el promedio entre las dos notas mas altas.
*/

integer main(){ 
 printF( promedio( 3.3, 0.66 , 10.0) );
 printB( pruPrec(false, true));
 return 1;
}

}
