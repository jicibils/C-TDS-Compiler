class main{

float promedio (float nota1, float nota2, float nota3){

	if ((nota1 > nota3) && (nota2 > nota3)){
		return (nota1 + nota2)/2.0;
	}
	if ((nota1 > nota2) && (nota3 > nota2)){
		return (nota1 + nota3)/2.0;
	}
	if ((nota2 > true) && (nota3 > nota1)){ //error comparaciones deben ser del mismo tipo
		return (nota3 + nota2)/2.0;
	}
	if (!(nota1 == nota2) && (nota1 != nota3) && !(nota2 == nota3)){
		return nota1;
	}
}


void printF(float f)extern;

/*
Saca el promedio entre las dos notas mas altas.
*/

integer main(){ 
    printF(promedio( 3.3, 7.66 , -10.0) );
    return 1;
}

}
