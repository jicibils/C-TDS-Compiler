class main{

integer A[5] ;
float B [56];
bool C[10];

integer pruChar(integer b, integer c ){
        integer i;
        A[0] = 5; 
        
        if ( A[0] < 3){
              return A[1];
        } 
}

void printI(integer i)extern;

integer main(){

    printI(pruArreglos(4,8 )); //ERROR metodo no declarado
    
    return 1;
    
}

}
