class main{
   float c;

   void alo(bool b, integer x){
      if ( b ) {
            x= x + 1;
      }
      else{
          x = x - 1;
      }  
      return ; 
   }

   integer alo2(integer x){
      alo(true, x); 
      x=  x + 1; 
      return x; 
   }

   bool alo2(bool x){ //error ya existe un metido con ese nombre
       
      x=  true && false; 
      return x; 
   }
 
 
   void main(){
        integer y;
        y = 7;
        c = y *8;
        y = alo2(y); 
        alo(false, (c+3)*y); 
        return;
   }

}
