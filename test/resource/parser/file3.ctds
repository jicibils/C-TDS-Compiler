class main {
	integer inc(integer x) {
		return x + 1;
	}
	integer read_int() extern;
	void print(bool s) extern;
	void main() {
		integer y;
		y = read_int();
		y = inc(y);
		if (y == 1) {
			print(y==1);
		}
		else {
			print(y!=1);
		}
	}
}